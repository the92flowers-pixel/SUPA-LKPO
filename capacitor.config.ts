import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.jurbamusiclkpo',
  appName: 'JURBA MUSIC LKPO',
  webDir: 'dist'
};

export default config;
